#include <iostream>

using namespace std;

int main(int argc, char const *argv[])
{
  // peso >= 115 && peso < 125
  // ch == 'q' || ch == 'Q'
  // !(x % 2) && x != 26
  // !(x % 2) && x % 26
  /*
    donativo >= 10 && donativo <= 20 || donativo >= 90 && donativo <= 100
  */
 // ch >= 'a' && ch <= 'z' || ch >= 'A' && ch <= 'Z'
  return 0;
}